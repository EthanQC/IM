server:
  http_port: 8081
  grpc_addr_identity: "127.0.0.1:9090"
  grpc_timeout: 3s
  read_timeout: 5s
  write_timeout: 5s

jwt:
  # 仅用于网关验证；必须与 identity_service 的签发密钥一致
  secret: "super-secret-change-me"
