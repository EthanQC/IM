# Secret 补丁：本地开发环境密钥
apiVersion: v1
kind: Secret
metadata:
  name: im-secrets
  namespace: im
type: Opaque
stringData:
  # MySQL 密码
  MYSQL_PASSWORD: "imdev"
  MYSQL_DSN: "root:imdev@tcp(host.docker.internal:3306)/im_db?charset=utf8mb4&parseTime=True&loc=Local"
  
  # JWT 密钥（开发环境）
  JWT_SECRET: "dev-jwt-secret-key-at-least-32-characters"
  
  # MinIO 凭证
  MINIO_ACCESS_KEY: "admin"
  MINIO_SECRET_KEY: "admin123"
